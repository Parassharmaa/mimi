name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-and-package:
    name: Test and package
    runs-on: macos-26
    # Universal Swift/MLX packaging currently takes about 35 minutes on the
    # hosted macOS runner. Leave headroom for cache misses and final artifact
    # cleanup instead of cancelling an otherwise successful job at 35:01.
    timeout-minutes: 50

    steps:
      - uses: actions/checkout@v7

      - name: Show Swift toolchain
        run: swift --version

      - name: Run deterministic tests
        run: scripts/test.sh

      - name: Build universal app archive
        run: scripts/package-app.sh

      - name: Verify packaged app
        run: |
          rm -rf .build/package-check
          mkdir -p .build/package-check
          ditto -x -k .build/dist/Mimi-macOS.zip .build/package-check
          codesign --verify --deep --strict .build/package-check/Mimi.app
          plutil -lint .build/package-check/Mimi.app/Contents/Info.plist
          python3 scripts/translation/verify_shipped_translation_pack.py \
            --app .build/package-check/Mimi.app
          ARCHS="$(lipo -archs .build/package-check/Mimi.app/Contents/MacOS/Mimi)"
          [[ "$ARCHS" == *arm64* && "$ARCHS" == *x86_64* ]]

      - name: Upload app archive
        uses: actions/upload-artifact@v7
        with:
          name: Mimi-macOS
          path: |
            .build/dist/Mimi-macOS.zip
            .build/dist/Mimi-macOS.zip.sha256
          if-no-files-found: error
          retention-days: 14
